# v2.30.1 (2026-05-12)

## 🐛 问题修复

### qa 插件命令命名空间修复

- 修复 qa 插件命令文件命名，移除重复的 `qa:` 前缀
  - `qa:init.md` → `init.md`，命令变为 `/qa:init`（而非 `/qa:qa:init`）
  - `qa:new.md` → `new.md`，命令变为 `/qa:new`
  - `qa:run.md` → `run.md`，命令变为 `/qa:run`
  - `qa:report.md` → `report.md`，命令变为 `/qa:report`
  - `qa:bug.md` → `bug.md`，命令变为 `/qa:bug`
  - 对齐 req 插件的命名规范

### .gitignore 更新

- 将 QA 测试报告和截图目录加入 `.gitignore`
  - `docs/qa/reports/`（测试执行报告）
  - `docs/qa/screenshots/`（测试截图证据）
  - flow 文档保留 git 追踪，以支持版本管理

## 📊 统计

- 文件变更：2 个（marketplace.json + .gitignore）
- bug 修复：3 处（qa 插件命名）

## 💾 兼容性

- ✅ 完全向后兼容
- ✅ 仅影响 qa 插件命令调用方式（修复为正确方式）
- ⚠️ 如之前按错误的 `/qa:qa:*` 方式调用，升级后请改为 `/qa:*`

# v2.13.1

> 发布日期：2026-04-19
> 版本范围：v2.12.0..HEAD（含 v2.13.0 准备但未 tag 的工作）

> 注：v2.13.0 此前已写入 changelog 和 prepare commit，但未推送 tag。本次发布将 v2.13.0 的内容并入 v2.13.1，作为统一发布点。原 `docs/changelogs/v2.13.0.md` 保留作为历史档。

## 重构

- **req 插件大文件按主题拆分，节约 prompt token**：
  - `release.md` 63KB → 43.8KB（-30%）：剥出 `release-rationale.md` (13.7KB) 收纳 draft 行为矩阵详解、push_tag_first 决策原理、跨分支流程设计、边界情况大表。常见路径每次省 ~7K tokens
  - `_common.md` 22KB → 6 个主题文件 + 2.4KB 索引页：
    - `_storage.md` (6.1KB)：settings + 路径 + 缓存 + 编号 + 元信息
    - `_template.md` (5.7KB)：状态确认 + 模板约束 + memory 隔离
    - `_issue.md` (4.1KB)：Issue 拉取 + 分支/commit 关联
    - `_granularity.md` (3.7KB)：粒度规则 + REQ/QUICK 选择
    - `_branch.md` (2.5KB)：分支策略配置
    - `_claude-md.md` (2.4KB)：CLAUDE.md 架构检查
  - 各命令按需 Read 单个主题文件，最高省 89% Read 体积
- 20 个 req 命令的 33 处 `[_common.md]` 引用 → 具体主题文件；2 个 skill (`dev-guide`、`natural-language-dispatcher`) 同步更新
- 旧链接 `[_common.md]` 仍能解析（指向新索引页 + 锚点映射表），向后兼容

## 文档

- 新增 `docs/design/token-optimization.md`：token 计算速算法、被算入 token 的环节、节约策略 7 招、维护 checklist、release.md 拆分案例对比
- `CLAUDE.md`「命令结构」章节追加 token 节约引用

## 行为变化（v2.13.0 准备的工作，本次一并发布）

- **确认拦截默认关闭**（req 3.9.0）：req 插件的 PreToolUse 确认不再默认弹框，Write/Edit/Bash 全部直通。命令已通过多轮讨论 / 显式参数 / y/n 在自己的流程里完成意图确认，叠加一层 Hook 原生对话框被视为冗余打断。
  - 删除 `confirm-before-write.sh`：需求文档 / PRD / 模板 / changelog 的 Write/Edit 不再拦截
  - `confirm-before-commit.sh` 改为按 marker 文件 `.claude/.req-confirm-commit` 存在性决定是否拦截 git commit / mv / rm 需求文件
- **commit 确认改为自然语言开关**：想恢复"commit 前弹框"只需对 Claude 说"开启提交确认"。Claude 会：
  - `touch .claude/.req-confirm-commit`（marker 文件）
  - 在 auto-memory 中落一条 feedback 记录偏好
  - 关闭同理——"不用确认了" → `rm` marker + 更新 memory
  - **整个开关过程不再需要手动编辑 `settings.local.json` 或任何配置文件**
- `/req:release` 在 changelog 覆盖时的行为也一并解除拦截：若 `docs/changelogs/<version>.md` 已存在则直接覆盖（需重新生成时请先手动备份或删除）。

## Bug 修复

- **`confirm-before-commit.sh` 仓库根解析**（req 3.9.0）：修复从子目录执行 `git commit` 时 marker 检查错位的 P1 bug（Codex 在 PR #34 提出）。改用 `git -C "$CWD" rev-parse --show-toplevel` 解析仓库根再拼 marker 路径，非 git 目录 fallback 到 CWD。同修复顺带覆盖 `.claude/.req-auto`。

## 文档（v2.13.0 准备的工作）

- `natural-language-dispatcher` 技能新增 §七「确认偏好开关」：首个"直接动作（非命令映射）"类目，Claude 据此识别"开启/关闭/查询提交确认"意图。
- 三份 tutorial（zh / en / ko）的 `/req:fix --auto` 章节同步更新前置说明、表格、底层机制，反映新的 marker 驱动模型。
- `CLAUDE.md`、`_common.md` 的确认操作规范段落改写为"默认直通 + 可选开启"。

## 版本升级

- marketplace：2.12.0 → 2.13.1
- req：3.8.0 → 3.10.0（含 v2.13.0 的 3.9.0 工作 + 本次拆分重构）
- api：0.3.0（不变）
- pm：0.2.0（不变）

# v2.29.0

**发布日期**：2026-05-09

## 功能增强

### 需求管理（req）

- **测试配置解耦**：`/req:test` 的测试命令、文件位置、环境启动命令全部改为从 `docs/prompt/testing.md` 读取，不再依赖 CLAUDE.md；文件缺失时回退到 `architecture.md` 测试规范章节（非阻塞）
- **init 骨架补全**：`/req:init` 步骤 8.6 新增 `testing.md` 骨架（标注 `/req:test` 读取此文件），`docs/prompt/` 总预置骨架数从 6 增至 7

## 提交范围

`v2.28.0..HEAD`（1 个 commit）
- `1971f98` feat(test): req:test 改从 docs/prompt/testing.md 读取测试细节

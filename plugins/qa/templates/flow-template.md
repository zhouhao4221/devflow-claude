# <功能名称> 测试流程

## 元信息

| 属性 | 值 |
|------|-----|
| 模块 | <module> |
| 操作方式 | browser |
| 入口 | http://localhost:3000 |
| 创建日期 | YYYY-MM-DD |
| 最后执行 | - |

> 操作方式可选值：`browser`（浏览器）/ `api`（纯接口）/ `api-then-verify`（API 造数 + browser 回查）/ `desktop`（桌面应用）

---

## 测试数据

全局复用的测试数据，场景中直接引用字段名：

| 字段 | 值 | 说明 |
|------|-----|------|
| 用户名 | test@example.com | 测试账号 |
| 密码 | test123456 | - |

---

## 已知结论

已验证、本次直接跳过的路径（执行时标记 SKIP，不重复验证）：

- （示例：`手工新增接口返回 500` — 已知 Bug，跳过 S02 手工新增场景）

---

## S01 <场景名称>

**前置条件**：已打开入口页面，用户未登录

**数据准备**（可选）：
- 方式：`api` — `POST /api/resource` 创建所需数据（参数见测试数据）
- 跳过条件：数据库已有目标数据时可省略

**步骤**：
1. 在 `[data-testid="xxx-input"]` 输入「测试值」
   - 等待信号：元素可交互（无 `disabled` 属性）
2. 点击 `[data-testid="yyy-button"]`
   - 等待信号：`「操作成功」` 文字出现 或 URL 变化
3. 在 `[data-testid="zzz-container"]` 确认结果

**预期结果**：
- `[data-testid="result-table"]` 包含文字「测试值」
- URL 为 `/target-path`

---

## S02 <场景名称>

**前置条件**：...

**数据准备**（可选）：
- 方式：`manual` / `script` / `已有数据`

**步骤**：
1. ...
   - 等待信号：...

**预期结果**：
- ...

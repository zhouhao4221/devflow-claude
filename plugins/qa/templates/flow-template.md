# <功能名称> 测试流程

## 元信息

| 属性 | 值 |
|------|-----|
| 模块 | <module> |
| 操作方式 | browser |
| 入口 | http://localhost:3000 |
| 创建日期 | YYYY-MM-DD |
| 最后执行 | - |

> 操作方式可选值：`browser`（浏览器）/ `desktop`（桌面应用）

---

## 测试数据

全局复用的测试数据，场景中直接引用字段名：

| 字段 | 值 | 说明 |
|------|-----|------|
| 用户名 | test@example.com | 测试账号 |
| 密码 | test123456 | - |

---

## 已知结论

已验证、本次直接跳过的路径（执行时标记 SKIP，不重复验证）：

- （示例：`手工新增接口返回 500` — 已知 Bug，跳过 S02 手工新增场景）

---

## S01 <场景名称>

**前置条件**：已打开入口页面，用户未登录

**数据准备**（可选）：
- 方式：`手动` — 在系统中提前创建所需数据（参见测试数据说明）
- 跳过条件：数据库已有目标数据时可省略

**数据清理**（新增场景必填）：
- 删除按钮选择器：`[data-testid="xxx-delete-button"]`
- 确认弹窗选择器：`[data-testid="confirm-ok-button"]`（或弹窗内文字「确认」）
- 触发：测试完成后自动通过 UI 执行，无论 PASS/FAIL

**步骤**：
1. 在 `[data-testid="xxx-input"]` 输入「测试值」
   - 等待信号：元素可交互（无 `disabled` 属性）
2. 点击 `[data-testid="yyy-button"]`
   - 等待信号：`「操作成功」` 文字出现 或 URL 变化
3. 在 `[data-testid="zzz-container"]` 确认结果

> **下拉 / 级联选择写法规范**（触发器与浮层选项必须分行写，不能合并）：
>
> ```
> 4. 点击触发器 `[data-testid="city-select"]`          ← 触发器（在容器内）
>    - 等待信号：浮层出现（body 全局，`.ant-select-dropdown` 可见）
> 5. 在浮层中点击文字「上海市」                           ← 浮层选项（在 body 全局找文字）
>    - 等待信号：浮层消失 或 触发器显示值变为「上海市」
> ```
>
> 级联选择（省 → 市 → 区）每层单独一步，每步写独立等待信号：
>
> ```
> 4. 点击触发器 `[data-testid="region-cascader"]`
>    - 等待信号：浮层出现（body 全局，`.ant-cascader-menus` 可见）
> 5. 在浮层第一列点击「广东省」
>    - 等待信号：浮层第二列出现（`.ant-cascader-menu:nth-child(2) li` 数量 > 0）
> 6. 在浮层第二列点击「广州市」
>    - 等待信号：浮层第三列出现（`.ant-cascader-menu:nth-child(3) li` 数量 > 0）
> 7. 在浮层第三列点击「天河区」
>    - 等待信号：浮层消失 或 触发器显示值变为「广东省/广州市/天河区」
> ```

**预期结果**：
- `[data-testid="result-table"]` 包含文字「测试值」
- URL 为 `/target-path`

---

## S02 <场景名称>

**前置条件**：...

**数据准备**（可选）：
- 方式：`手动` / `已有数据`

**步骤**：
1. ...
   - 等待信号：...

**预期结果**：
- ...

---

## SXX 表单边界 & 字符类型（推荐随业务场景同步补充）

> 对每个有「新增 / 编辑」表单的功能，补充此场景。

**前置条件**：已打开表单，处于可输入状态

**字段清单**（按实际表单填写）：

| 字段 | 选择器 | 最大长度 | 最小长度 | 字段类型约束 |
|------|--------|---------|---------|------------|
| 客户名称 | `[data-testid="customer-name-input"]` | 50 | 1 | 中英文数字 |
| 手机号 | `[data-testid="customer-phone-input"]` | 11 | 11 | 数字 |

**测试矩阵**（对每个字段逐一执行）：

| # | 输入类型 | 示例输入 | 预期结果 |
|---|---------|---------|---------|
| B1 | 空值 / 必填 | （清空） | 内联提示「XX 不能为空」，不提交 |
| B2 | 最大长度 + 1 | 超出 maxLength 1 位的字符串 | 截断输入 或 提示超限，不提交 |
| B3 | 最小长度 - 1 | 少于 minLength 1 位 | 提示长度不足，不提交 |
| C1 | 纯中文 | 张三李四 | 正常接受并保存 |
| C2 | 中英文混合 | abc张三 | 正常接受并保存 |
| C3 | 特殊字符 | `< > & " '` | 正常保存（展示时 HTML 转义，不破坏页面） |
| C4 | 潜在 XSS | `<script>alert(1)</script>` | 正常保存，展示为文本，脚本不执行 |
| C5 | 首尾空格 | ` 测试 ` | 自动 trim 或原样保留（明确业务规则） |
| C6 | Emoji | 😀🎉 | 正常接受 或 提示不支持（视业务规则） |
| C7 | 全角字符 | ａｂｃ１２３ | 正常接受 或 提示仅支持半角（视业务规则） |

> 按需裁剪：不适用的行可删除；字段有特殊约束（仅数字、手机格式等）需额外补充。

**步骤**（对每个字段，按测试矩阵逐行执行）：
1. 清空 `[data-testid="<field-input>"]`，输入当前测试行的示例值
2. 点击提交按钮
   - 等待信号：提示文字出现 或 表单关闭 或 URL 变化
3. 记录实际结果，对比预期

**预期结果**：
- B1-B3（边界）：验证失败时表单内联提示，不触发接口请求
- C1-C7（字符）：合法字符正常保存；详情页展示不乱码、不执行脚本、不破坏布局
